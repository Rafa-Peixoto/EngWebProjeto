extends layout

block content
  h1 Lista de UCs
  .search-bar
    input(type="text" placeholder="Pesquisar nos seus cursos")
  
  .container
    each uc in ucs
      .uc-card
        h2= uc.titulo
        p Docentes: #{uc.docentes.join(', ')}
        a(href=`/ucs/${uc.sigla}`) Mais informações
